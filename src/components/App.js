import React from "react";
import Homepage from "./HomePage"

class App extends React.Component {
    render() {
        return <div>
            <Homepage />
            <p class="rotate"> Need help ? </p>
        </div>
    }
}

export default App;